[project]
name = "zammad-ui-test"
version = "0.1.0"
description = "Playwright UI test for Zammad laptop-refresh conversation"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-playwright>=0.7.0",
    "playwright>=1.40.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
